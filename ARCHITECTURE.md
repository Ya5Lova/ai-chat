# システム構成・設計解説

> **対象**: Next.js / Hono / Mastra を初めて触る初心者の方  
> このファイルを読めば「なぜこの構成なのか」「各技術が何をしているのか」がわかります。

---

## 全体像を一言で言うと

```
ユーザーがブラウザで話しかける
    → Next.js がUI（画面）を表示する
    → Hono がリクエストを受け取ってAIに渡す
    → Mastra がずんだもんとしてClaude AIに質問する
    → 返答がブラウザにリアルタイムで流れてくる
```

これだけです。順番に詳しく説明していきます。

---

## 登場人物の紹介

### 1. Next.js — 「画面を作る人」

**Next.js とは？**  
React ベースの Web フレームワークです。  
「フレームワーク」とは、アプリ開発に必要な仕組みをまとめたツールセットのことです。

**このプロジェクトでの役割**

- チャット画面（HTML/CSS）を作って表示する
- ユーザーが入力したメッセージを受け取る
- AIからの返答を画面にリアルタイム表示する

**App Router とは？**  
Next.js 13 以降の新しいルーティング方式です。  
`app/` フォルダの中にファイルを置くだけでページやAPIが作れます。

```
app/
├── page.tsx        →  http://localhost:3000/  （チャット画面）
└── api/
    └── chat/
        └── route.ts →  http://localhost:3000/api/chat  （AIとの通信口）
```

---

### 2. Hono — 「受付係」

**Hono とは？**  
軽量で高速な Web API フレームワークです。  
Express.js に似ていますが、より現代的でシンプルです。

**このプロジェクトでの役割**

ブラウザから送られてくるメッセージを受け取り、Mastra に渡す「受付係」です。

```
ブラウザ  →  POST /api/chat  →  Hono  →  Mastra
```

**なぜ Next.js だけじゃダメなの？**  
実は Next.js のAPI Routes だけでも動きます。  
Hono を使う理由は：

- ルーティングをきれいに整理できる
- バリデーション（入力チェック）が書きやすい
- 将来APIを増やすときに拡張しやすい

このプロジェクトでは `app/api/chat/route.ts` の中で Hono を呼び出す構成にします。

---

### 3. Mastra — 「AIエージェントの司令塔」

**Mastra とは？**  
AIエージェントを作るための TypeScript フレームワークです。  
「AIエージェント」とは、ただ質問に答えるだけでなく、記憶を持ったり道具を使ったりできるAIのことです。

**このプロジェクトでの役割**

- ずんだもんのキャラクター設定（システムプロンプト）を管理する
- 会話の流れ（文脈）を記憶する
- Claude AI（実際に言葉を生成するモデル）を呼び出す

**システムプロンプトとは？**  
AIに最初に渡す「キャラクター設定書」のようなものです。

```
「あなたはずんだもんです。語尾に〜のだをつけてください...」
```

この設定があるから、AIはずんだもんとして振る舞えます。

---

### 4. Claude — 「実際に言葉を生成するAI」

Anthropic が開発したAIモデルです。  
このプロジェクトでは `claude-sonnet-4-6` を使います。

Mastra が「橋渡し役」として Claude を呼び出します。  
ユーザーには見えない裏側の部分です。

---

## データの流れ（詳細版）

```
① ユーザーが「こんにちは！」と入力して送信ボタンを押す

② ブラウザ（Next.js）が APIにリクエストを送る
   POST /api/chat
   Body: { message: "こんにちは！", threadId: "abc-123" }

③ Hono がリクエストを受け取り、入力値を確認してMastraに渡す

④ Mastra がシステムプロンプト（ずんだもん設定）＋会話履歴＋メッセージを
   まとめて Claude API に送る

⑤ Claude が「こんにちは！ぼくはずんだもんなのだ！」と生成し始める

⑥ 生成された文字がリアルタイムでブラウザに「流れてくる」（ストリーミング）

⑦ 画面に文字が1文字ずつ表示されていく
```

---

## ストリーミングとは？

普通のAPIは「全部生成されてから一気に返す」方式です。  
ストリーミングは「生成しながらリアルタイムで送る」方式です。

```
通常:   [待つ...........] ドーン！「こんにちはなのだ！」
ストリーム: 「こ」「ん」「に」「ち」「は」「な」「の」「だ」「！」 ← 1文字ずつ届く
```

チャットアプリのタイピング風表示はこの仕組みで動いています。  
技術的には **SSE（Server-Sent Events）** という仕組みを使います。

---

## 会話履歴の管理

このアプリでは **セッション中のみ** 会話を覚えます。  
データベースは使いません。

```
クライアント側（ブラウザ）
└── useState でメッセージ配列を保持
    → ページをリロードしたら消える

サーバー側（Mastra）
└── threadId でスレッドを識別
    → サーバーが再起動したら消える
```

**threadId とは？**  
会話のIDです。ブラウザが最初に `crypto.randomUUID()` で生成し、  
毎回のリクエストに含めることで「同じ会話の続き」だとMastraが認識できます。

---

## ファイル構成と役割

```
ai-chat/
│
├── app/                      ← Next.js App Router のルートディレクトリ
│   ├── layout.tsx            ← 全ページ共通のHTMLラッパー（<head>など）
│   ├── page.tsx              ← チャット画面本体（ここがメイン画面）
│   └── api/
│       └── chat/
│           └── route.ts      ← /api/chat エンドポイント（Honoを呼ぶ）
│
├── lib/
│   ├── hono.ts               ← Honoのルーター定義（受付処理）
│   └── mastra/
│       ├── index.ts          ← Mastraの初期化（設定ファイル）
│       └── agent.ts          ← ずんだもんエージェントの定義
│
├── components/               ← 画面を構成するパーツ（UIコンポーネント）
│   ├── chat-window.tsx       ← メッセージ一覧を表示するエリア
│   ├── message-bubble.tsx    ← メッセージ1件分の吹き出し
│   └── input-bar.tsx         ← テキスト入力欄＋送信ボタン
│
└── public/
    └── zundamon.png          ← ずんだもんのキャラクター画像
```

---

## 各技術の関係図

```
┌─────────────────────────────────────────────────────┐
│                   ブラウザ                           │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           Next.js (表示・UI)                │   │
│  │                                             │   │
│  │  chat-window.tsx  message-bubble.tsx        │   │
│  │  input-bar.tsx    page.tsx                  │   │
│  └──────────────────────┬──────────────────────┘   │
└─────────────────────────┼───────────────────────────┘
                          │ HTTP リクエスト
                          ▼
┌─────────────────────────────────────────────────────┐
│                   サーバー                           │
│                                                     │
│  ┌──────────────────┐                              │
│  │   Hono（受付）    │  /api/chat を受け取る        │
│  └────────┬─────────┘                              │
│           │                                         │
│           ▼                                         │
│  ┌──────────────────────────────────────┐          │
│  │   Mastra（AIエージェント管理）        │          │
│  │   - ずんだもんシステムプロンプト      │          │
│  │   - 会話履歴（in-memory）            │          │
│  └────────────────────┬─────────────────┘          │
│                       │                             │
└───────────────────────┼─────────────────────────────┘
                        │ Claude API 呼び出し
                        ▼
              ┌──────────────────┐
              │  Claude AI       │
              │  (Anthropic)     │
              │  言葉を生成する  │
              └──────────────────┘
```

---

## デプロイ構成（本番環境）

ローカルでは `npm run dev` で開発しますが、  
本番公開では **Google Cloud Run** を使います。

```
ローカル開発
└── npm run dev → http://localhost:3000

本番環境
└── Docker イメージ
    └── Google Cloud Run
        └── https://your-app.run.app
```

**Cloud Run とは？**  
Dockerコンテナをサーバーなしで動かせるGoogleのサービスです。  
アクセスがないときはコストがかかりません（スケールtoゼロ）。

---

## まとめ：なぜこの構成なのか

| 選択             | 理由                                               |
|----------------|----------------------------------------------------|
| Next.js        | UIとAPIを1つのプロジェクトで管理できる             |
| Hono           | シンプルで軽量。APIルーティングが書きやすい         |
| Mastra         | AIエージェントの記憶・キャラ設定管理が簡単         |
| Claude         | 日本語が得意で、キャラクターの演技が上手なAI       |
| セッションメモリ | DBが不要でシンプル。エンタメ用途には十分           |
| Cloud Run      | サーバー管理不要。アクセス数に応じて自動スケール   |

---

## 次のステップ

1. `npm create next-app` でプロジェクトを初期化する
2. Hono / Mastra をインストールする
3. ずんだもんエージェントを定義する
4. チャットUIを作る
5. つなぎ合わせてローカルで動作確認する
6. Dockerファイルを作ってCloud Runにデプロイする
